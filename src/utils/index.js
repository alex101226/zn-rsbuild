import {formatDate, coverDateString, srcset, coverDateObj} from './format'
import { http } from './fetch'
import { message } from './message'
import { transNumber } from './trans'
import {
  randomDate, randomLocation, randomStatus, randomRemark,
  randomFactoryLocation, dateRange, randomName, randomCoordinate,
  randomFireUpsStatus, randomSosStatus, getUsageRate
} from './mockCommon'

export {
  formatDate,
  coverDateString,
  srcset,
  coverDateObj,
  http,
  message,
  randomDate,
  randomLocation,
  randomStatus,
  randomRemark,
  randomFactoryLocation,
  randomName,
  randomCoordinate,
  randomFireUpsStatus,
  randomSosStatus,
  getUsageRate,
  dateRange,
  transNumber
}